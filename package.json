{
  "name": "test",
  "version": "1.0.0",
  "main": "./dist-electron/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "tsc && tsc-alias",
    "build": "npm run dist && cross-env NODE_ENV=production electron .",
    "dev": "npm run dist && cross-env NODE_ENV=development electron ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/electron": "^1.6.12",
    "@types/node": "^22.13.10",
    "@types/puppeteer": "^5.4.7",
    "@types/typescript": "^2.0.0",
    "cross-env": "^7.0.3",
    "electron": "^35.0.1",
    "tsc-alias": "^1.8.11",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "puppeteer": "^24.4.0"
  }
}
